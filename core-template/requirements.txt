# KRATOS Core — dependências base (genéricas)
fastapi>=0.110
uvicorn[standard]>=0.27      # dev local: uvicorn main:app --reload
a2wsgi>=1.10                 # ASGI→WSGI (Passenger/cPanel)
python-dotenv>=1.0
reportlab>=4.0              # relatório PDF
python-docx>=1.1           # relatório DOCX
# TODO-DOMINIO: adicione aqui as libs da sua fonte de dados (ex.: websockets, httpx, etc.)
