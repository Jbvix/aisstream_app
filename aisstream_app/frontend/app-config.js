window.APP_CONFIG = {
  // Para desenvolvimento local com FastAPI em localhost:8080, deixe vazio.
  // Para Netlify, preencha com o endpoint publico do seu backend (Render/Railway/Fly/etc).
  API_BASE: "",

  // WebSocket do backend. Em dev local, deixe vazio para autodetectar.
  // Exemplo: "wss://seu-backend.exemplo.com/ws"
  WS_URL: ""
};
