---
# Tasks via .cpanel.yml (Git Version Control nem sempre mostra "Deployment Script").
deployment:
  tasks:
    - export DEPLOYPATH=/home/c62gtwye66po/aisstream_app
    - cd "$DEPLOYPATH"
    - /bin/mkdir -p tmp frontend
    - /bin/touch tmp/restart.txt
